# =====================================================
# 🌌 DREAMCODER STARSHIP v6.0 - P10K REINCARNATION
# =====================================================
# Design: Segmented Powerline (P10k Aesthetic)
# Philosophy: High contrast, solid blocks, consistent look.
#
# Separators:    
# Palette: Dreamcoder Neon Cyber (Segmented)
# =====================================================

"$schema" = 'https://starship.rs/config-schema.json'

# --------------------------------------------------------------------------
# 🎯 PROMPT LAYOUT
# --------------------------------------------------------------------------
# We use custom grouping to simulate P10k segments
format = """
[](#00e5ff)
$os
$username
[](fg:#00e5ff bg:#4da6b8)
$directory
[](fg:#4da6b8 bg:#ff6b9f)
$git_branch
$git_status
[](fg:#ff6b9f bg:#ffd166)
$rust
$nodejs
$bun
$golang
$python
[](fg:#ffd166)
$character"""

# Right prompt for Time and Battery
right_format = """[](fg:#d197ff)$time[](fg:#d197ff)"""

add_newline = true
command_timeout = 1000

# --------------------------------------------------------------------------
# ⚡ CHARACTER
# --------------------------------------------------------------------------
[character]
success_symbol = "[ ❯](bold #00e5ff)"
error_symbol = "[ ❯](bold #ff6b9f)"

# --------------------------------------------------------------------------
# 🖥️ OS & USER (Segment 1: Neon Cyan)
# --------------------------------------------------------------------------
[os]
format = "[$symbol]($style)"
style = "bg:#00e5ff fg:#000000"
disabled = false

[os.symbols]
Arch = "󰣇 "
Ubuntu = "󰕈 "
Macos = "󰀵 "
Windows = "󰍲 "
Linux = "󰌽 "

[username]
show_always = true
format = "[$user]($style)"
style_user = "bg:#00e5ff fg:#000000 bold"
style_root = "bg:#ff6b9f fg:#000000 bold"

# --------------------------------------------------------------------------
# 📂 DIRECTORY (Segment 2: Cyan Water)
# --------------------------------------------------------------------------
[directory]
truncation_length = 3
truncation_symbol = "…/"
style = "bg:#4da6b8 fg:#000000 bold"
format = " [$path]($style) "
home_symbol = "🏠"

# --------------------------------------------------------------------------
# 🌿 GIT (Segment 3: Neon Pink)
# --------------------------------------------------------------------------
[git_branch]
symbol = " "
style = "bg:#ff6b9f fg:#000000 bold"
format = " [$symbol$branch]($style)"

[git_status]
style = "bg:#ff6b9f fg:#000000 bold"
format = "[[($all_status$ahead_behind)]]($style) "
conflicted = "🏳️"
ahead = "🏎️💨"
behind = "🐢"
diverged = "🔱"
untracked = "📝"
stashed = "📦"
modified = "✍️"
staged = "P"
renamed = "»"
deleted = "🗑️"

# --------------------------------------------------------------------------
# 🟢 RUNTIMES (Segment 4: Gold)
# --------------------------------------------------------------------------
[rust]
symbol = "🦀 "
style = "bg:#ffd166 fg:#000000 bold"
format = " [$symbol($version)]($style)"

[nodejs]
symbol = "󰎘 "
style = "bg:#ffd166 fg:#000000 bold"
format = " [$symbol($version)]($style)"

[bun]
symbol = "󰡘 "
style = "bg:#ffd166 fg:#000000 bold"
format = " [$symbol($version)]($style)"

[golang]
symbol = "󰟓 "
style = "bg:#ffd166 fg:#000000 bold"
format = " [$symbol($version)]($style)"

[python]
symbol = "󰌠 "
style = "bg:#ffd166 fg:#000000 bold"
format = " [${symbol}${version}]($style)"

# --------------------------------------------------------------------------
# ⏱️ TIME (Right Segment: Purple)
# --------------------------------------------------------------------------
[time]
disabled = false
format = "[$time]($style)"
time_format = " %H:%M "
style = "bg:#d197ff fg:#000000 bold"

# --------------------------------------------------------------------------
# 🌌 "The Soul of P10k in the Body of Starship"
#      - DreamCoder v6.0
# --------------------------------------------------------------------------
