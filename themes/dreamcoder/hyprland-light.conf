# Dreamcoder color layer for Hyprland.
# Import after ML4W/Gentleman defaults; this changes colors only.

general {
    col.active_border = rgba(805313ff) rgba(176875ff) 45deg
    col.inactive_border = rgba(b8afa1df)
}

misc {
    background_color = rgba(fbfaf7ff)
}
