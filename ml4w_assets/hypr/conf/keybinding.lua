-- Keybinding variant selector
-- Dreamcoder contract: the curated variant (dreamcoder.lua) only holds native
-- binds the profile generator cannot emit. The profile JSON (custom.lua) owns
-- all keyboard binds. See README "Binding contract".
local name = "dreamcoder.lua"
load_variant(name,"keybindings")
