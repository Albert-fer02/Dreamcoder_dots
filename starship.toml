# =====================================================
# 🚀 ELITE STARSHIP CONFIGURATION
# =====================================================
# Advanced prompt configuration for maximum productivity

"$schema" = 'https://starship.rs/config-schema.json'

format = """
$os$username$directory$git_branch$git_status$git_metrics$nodejs$bun$rust$python$golang$java$docker_context$package$jobs$cmd_duration$status
$character"""

add_newline = false
command_timeout = 500

[character]
success_symbol = "[❯](bold #50C7E3)"
error_symbol = "[❯](bold #FF6B6B)"
vicmd_symbol = "[❮](bold #FFD93D)"

[os]
format = "[$symbol ]($style)"
style = "bold #6A9BD1"
disabled = false

[os.symbols]
Arch = "󰣇"
Ubuntu = "󰕈"
Debian = "󰣚"
Fedora = "󰣛"
Macos = "󰀵"
Windows = "󰍲"

[username]
style_user = "bold #A8DADC"
style_root = "bold #FF6B6B"
format = "[$user]($style)[/](bold #6A9BD1)"
disabled = false
show_always = true


[directory]
truncation_length = 3
truncation_symbol = "…/"
read_only = " "
style = "bold #50C7E3"
format = "[$path]($style)[$read_only](bold #FF6B6B)"
home_symbol = "~"
fish_style_pwd_dir_length = 1
use_logical_path = true

[directory.substitutions]
"Documents" = "󰈙"
"Downloads" = "󰇚"
"Music" = "󰝚"
"Pictures" = "󰋩"
"Projects" = "󰲋"
"Desktop" = "󰖯"

[git_branch]
symbol = " "
style = "bold #A8DADC"
format = " [$symbol$branch]($style)"
truncation_length = 15
truncation_symbol = "…"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold #FFD93D"
conflicted = "⚠️"
ahead = "↑${count}"
behind = "↓${count}"
diverged = "↕️↑${ahead_count}↓${behind_count}"
up_to_date = "✓"
untracked = "?${count}"
stashed = "󰆉"
modified = "~${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "-${count}"

[git_metrics]
added_style = "bold #50C7E3"
deleted_style = "bold #FF6B6B"
only_nonzero_diffs = true
format = '([+$added]($added_style) )([-$deleted]($deleted_style) )'
disabled = false

[nodejs]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #50C7E3"
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]

[bun]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #FFD93D"
detect_files = ["bun.lockb", "bunfig.toml"]

[rust]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #FF6B6B"
detect_files = ["Cargo.toml"]

[python]
format = ' [${symbol}${version}(\($virtualenv\))]($style)'
symbol = " "
style = "bold #FFD93D"
pyenv_version_name = false
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "__init__.py", "pyproject.toml", "requirements.txt", "setup.py", "tox.ini"]

[docker_context]
format = " [$symbol$context]($style)"
symbol = " "
style = "bold #6A9BD1"
only_with_files = true
detect_files = ["docker-compose.yml", "docker-compose.yaml", "Dockerfile"]

[aws]
format = ' [$symbol$profile(\($region\))]($style)'
symbol = " "
style = "bold #A8DADC"
disabled = false
expiry_symbol = "⚠️"
force_display = false

[gcloud]
format = ' [$symbol$account(\($region\))]($style)'
symbol = " "
style = "bold #50C7E3"

[kubernetes]
format = ' [$symbol$context(\($namespace\))]($style)'
symbol = " "
style = "bold #6A9BD1"
disabled = false
detect_files = ["k8s", "kustomization.yaml", "kustomization.yml"]
detect_folders = [".kube"]

[terraform]
format = " [$symbol$workspace]($style)"
symbol = " "
style = "bold #A8DADC"

[package]
format = " [$symbol$version]($style)"
symbol = " "
style = "bold #FFD93D"
display_private = false

[cmd_duration]
min_time = 1000
format = " [$duration]($style)"
style = "bold #A8DADC"
show_milliseconds = false
show_notifications = false

[status]
style = "bold #FF6B6B"
symbol = "✗"
success_symbol = "✓"
not_executable_symbol = "⛔"
not_found_symbol = "❓"
sigint_symbol = "⚠️"
signal_symbol = "⚡"
format = ' [$symbol$maybe_int]($style)'
map_symbol = false
disabled = false

[memory_usage]
disabled = true
threshold = 70
symbol = "󰍛"
style = "bold dimmed red"
format = "$symbol [${ram}( | ${swap})]($style) "

[time]
disabled = true
format = '󰥔[\[ $time \]]($style) '
time_format = "%T"
utc_time_offset = "local"
time_range = "10:00:00-14:00:00"

[battery]
full_symbol = "󰁹"
charging_symbol = "󰂄"
discharging_symbol = "󰂃"
unknown_symbol = "󰂑"
empty_symbol = "󰂎"

[[battery.display]]
threshold = 30
style = "bold red"

[[battery.display]]
threshold = 60
style = "bold yellow"

[[battery.display]]
threshold = 100
style = "bold green"

[golang]
format = " [$symbol$version]($style)"
symbol = " "
style = "bold #50C7E3"
detect_files = ["go.mod", "go.sum", "glide.yaml", "Gopkg.yml", "Gopkg.lock", ".go-version"]
detect_folders = ["Godeps"]
detect_extensions = ["go"]

[java]
format = " [$symbol$version]($style)"
symbol = " "
style = "bold #FFD93D"
detect_extensions = ["java", "class", "jar", "gradle", "clj", "cljc"]
detect_files = ["pom.xml", "build.gradle.kts", "build.sbt", ".java-version", ".deps.edn", "project.clj", "build.boot"]

[jobs]
symbol = " "
style = "bold #FF6B6B"
number_threshold = 1
symbol_threshold = 1
format = " [$symbol$number]($style)"

[shlvl]
disabled = true
format = "$shlvl level(s) down"
threshold = 3

[shell]
fish_indicator = "󰈺"
powershell_indicator = "󰈺"
bash_indicator = "󰞀"
zsh_indicator = "󰺧"
disabled = false

# Security tools detection (disabled for performance)
[custom.nuclei]
disabled = true

[custom.subfinder]
disabled = true

[custom.httpx]
disabled = true

# Development environment detection
[custom.project_type]
disabled = true

# Network status (disabled for performance)
# Custom modules disabled for performance

[custom.vpn]
disabled = true