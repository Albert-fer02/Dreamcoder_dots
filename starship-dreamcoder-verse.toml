# =====================================================
# 🌌 DREAMCODER VERSE STARSHIP PROMPT - PRODUCTIVE
# =====================================================
# Soft contrast meets high productivity
# Inspired by DreamCoder Kitty color palette
#
# Aesthetic: Professional × Minimal × Functional
# Palette: Soft terracotta, aqua-green, muted blues, gentle lavender
# =====================================================

"$schema" = 'https://starship.rs/config-schema.json'

# =====================================================
# 🎯 PRODUCTIVE PROMPT FORMAT (Single Line)
# =====================================================
# Layout: Clean and information-dense for maximum productivity
format = """$os\
$username\
$directory\
$git_branch\
$git_status\
$git_metrics\
$nodejs\
$bun\
$rust\
$python\
$golang\
$java\
$docker_context\
$kubernetes\
$package\
$cmd_duration\
$jobs\
$status\
$line_break\
$character"""

add_newline = false
command_timeout = 500
scan_timeout = 30

# =====================================================
# ⚡ CHARACTER (Productive Cursor)
# =====================================================
[character]
success_symbol = "[❯](bold #83d3e3)"     # Cyan claro (kitty selection)
error_symbol = "[❯](bold #D49A7A)"       # Terracota suave (kitty color1)
vicmd_symbol = "[❮](bold #E0C180)"       # Dorado suave (kitty color3)
vimcmd_symbol = "[❮](bold #C5A5D4)"      # Lila suave (kitty color5)
vimcmd_visual_symbol = "[V](bold #7fb3d4)" # Azul suave (kitty color4)
vimcmd_replace_symbol = "[R](bold #D49A7A)"

# =====================================================
# 🖥️ OS INDICATOR (Clean Identity)
# =====================================================
[os]
format = "[$symbol]($style)"
style = "bold #7fb3d4"  # Azul suave (kitty color4)
disabled = false

[os.symbols]
Arch = "󰣇 "
Ubuntu = "󰕈 "
Debian = "󰣚 "
Fedora = "󰣛 "
Macos = "󰀵 "
Windows = "󰍲 "
Linux = "󰌽 "

# =====================================================
# 👤 USERNAME (Professional Identity)
# =====================================================
[username]
style_user = "bold #83d3e3"      # Cyan claro (kitty selection)
style_root = "bold #D49A7A"      # Terracota suave (kitty color1)
format = "[$user]($style) "
disabled = false
show_always = true

# =====================================================
# 📂 DIRECTORY (Clean Navigation)
# =====================================================
[directory]
truncation_length = 3
truncation_symbol = "…/"
read_only = " 󰌾"
style = "bold #4da6b8"           # Cyan (kitty color6)
format = "[$path]($style)[$read_only](bold #D49A7A)"
home_symbol = "~"
fish_style_pwd_dir_length = 1
use_logical_path = true
repo_root_style = "bold #C5A5D4" # Lila suave para git roots

[directory.substitutions]
"Documents" = "󰈙 docs"
"Documentos" = "󰈙 docs"
"Downloads" = "󰇚 down"
"Music" = "󰝚 music"
"Pictures" = "󰋩 pics"
"Projects" = "󰲋 proj"
"PROYECTOS" = "󰲋 proj"
"Desktop" = "󰖯 desk"
".config" = " cfg"
"node_modules" = " nm"

# =====================================================
# 🌿 GIT BRANCH (Version Control)
# =====================================================
[git_branch]
symbol = " "
style = "bold #C5A5D4"           # Lila suave (kitty color5)
format = " [$symbol$branch]($style)"
truncation_length = 15
truncation_symbol = "…"
always_show_remote = false

# =====================================================
# 📊 GIT STATUS (Status Indicators)
# =====================================================
[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold #E0C180"           # Dorado suave (kitty color3)
conflicted = "⚡"
ahead = "↑${count}"
behind = "↓${count}"
diverged = "↕${ahead_count}↓${behind_count}"
up_to_date = "✓"
untracked = "?${count}"
stashed = "󰆉"
modified = "~${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "-${count}"

# =====================================================
# 📈 GIT METRICS (Code Changes)
# =====================================================
[git_metrics]
added_style = "bold #7FB3A8"     # Verde-agua suave (kitty color2)
deleted_style = "bold #D49A7A"   # Terracota suave (kitty color1)
only_nonzero_diffs = true
format = '([+$added]($added_style) )([-$deleted]($deleted_style) )'
disabled = false
ignore_submodules = true

# =====================================================
# 🟢 NODE.JS (Green Runtime)
# =====================================================
[nodejs]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #7FB3A8"           # Verde-agua suave (kitty color2)
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]

# =====================================================
# 🟡 BUN (Gold Runtime)
# =====================================================
[bun]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #E0C180"           # Dorado suave (kitty color3)
detect_files = ["bun.lockb", "bunfig.toml"]

# =====================================================
# 🦀 RUST (Terracotta Metal)
# =====================================================
[rust]
format = " [$symbol$version]($style)"
version_format = "${raw}"
symbol = " "
style = "bold #D49A7A"           # Terracota suave (kitty color1)
detect_files = ["Cargo.toml", "Cargo.lock"]

# =====================================================
# 🐍 PYTHON (Aqua Snake)
# =====================================================
[python]
format = ' [${symbol}${version}($virtualenv)]($style)'
symbol = " "
style = "bold #7FB3A8"           # Verde-agua (kitty color2)
pyenv_version_name = false
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "pyproject.toml", "requirements.txt"]

# =====================================================
# 🐹 GOLANG (Cyan Gopher)
# =====================================================
[golang]
format = " [$symbol$version]($style)"
symbol = " "
style = "bold #4da6b8"           # Cyan (kitty color6)
detect_files = ["go.mod", "go.sum"]
detect_folders = ["Godeps"]
detect_extensions = ["go"]

# =====================================================
# ☕ JAVA (Lavender Coffee)
# =====================================================
[java]
format = " [$symbol$version]($style)"
symbol = " "
style = "bold #C5A5D4"           # Lila suave (kitty color5)
detect_extensions = ["java", "class", "jar"]
detect_files = ["pom.xml", "build.gradle.kts"]

# =====================================================
# 🐳 DOCKER (Soft Blue Container)
# =====================================================
[docker_context]
format = " [$symbol$context]($style)"
symbol = " "
style = "bold #7fb3d4"           # Azul suave (kitty color4)
only_with_files = true
detect_files = ["docker-compose.yml", "Dockerfile"]

# =====================================================
# ☸️ KUBERNETES (Lavender Cluster)
# =====================================================
[kubernetes]
format = ' [$symbol$context(\($namespace\))]($style)'
symbol = "󱃾 "
style = "bold #C5A5D4"           # Lila suave (kitty color5)
disabled = false
detect_files = ["k8s", "kustomization.yaml"]
detect_folders = [".kube"]

[kubernetes.context_aliases]
"dev.local.cluster.k8s" = "dev"
".*/openshift-cluster/.*" = "openshift"

# =====================================================
# ⏱️ CMD DURATION (Execution Time)
# =====================================================
[cmd_duration]
min_time = 1000
format = " [$duration]($style)"
style = "bold #83d3e3"           # Cyan claro (kitty selection)
show_milliseconds = false
show_notifications = false

# =====================================================
# 🔴 STATUS (Error Indicator)
# =====================================================
[status]
style = "bold #D49A7A"           # Terracota (kitty color1)
symbol = "✗"
success_symbol = ""
not_executable_symbol = "⛔"
not_found_symbol = "?"
sigint_symbol = "⚠"
signal_symbol = "⚡"
format = ' [$symbol$maybe_int]($style)'
map_symbol = false
disabled = false

# =====================================================
# 💼 BACKGROUND JOBS (Process Count)
# =====================================================
[jobs]
symbol = " "
style = "bold #D49A7A"           # Terracota (kitty color1)
number_threshold = 1
symbol_threshold = 1
format = " [$symbol$number]($style)"

# =====================================================
# 📦 PACKAGE VERSION (Dependencies)
# =====================================================
[package]
format = " [$symbol$version](dimmed $style)"
symbol = " "
style = "#C5A5D4"                # Lila suave (kitty color5)
display_private = false
disabled = true  # Disabled for performance

# =====================================================
# 🔋 BATTERY (Energy Core - Optional)
# =====================================================
[battery]
full_symbol = "󰁹"
charging_symbol = "󰂄"
discharging_symbol = "󰂃"
unknown_symbol = "󰂑"
empty_symbol = "󰂎"
disabled = true  # Enable if on laptop

[[battery.display]]
threshold = 20
style = "bold #FF0080"
discharging_symbol = "󰂃"

[[battery.display]]
threshold = 50
style = "bold #FFD700"
discharging_symbol = "󰁿"

[[battery.display]]
threshold = 100
style = "bold #4FD1C5"
discharging_symbol = "󰁹"

# =====================================================
# 🕐 TIME (Temporal Coordinates - Optional)
# =====================================================
[time]
disabled = true  # Enable if you want time display
format = '[󰥔 $time](bold dimmed #718096)'
time_format = "%H:%M"
utc_time_offset = "local"

# =====================================================
# 🧠 MEMORY USAGE (Neural Load - Optional)
# =====================================================
[memory_usage]
disabled = true  # Enable for system monitoring
threshold = 75
symbol = "󰍛 "
style = "bold dimmed #FC8181"
format = "$symbol[$ram( | $swap)]($style)"

# =====================================================
# 🛡️ SHELL INDICATOR (Runtime Environment)
# =====================================================
[shell]
fish_indicator = "󰈺 fish"
powershell_indicator = "󰨊 pwsh"
bash_indicator = "󰞀 bash"
zsh_indicator = "󰰷 zsh"
disabled = true  # Usually not needed

# =====================================================
# 🎨 CUSTOM MODULES (DreamCoder Extensions)
# =====================================================

# Cosmic Separator (optional visual enhancement)
[custom.separator]
disabled = true
when = true
command = "echo ''"
format = "[◊](bold #2D3748)"
style = "#2D3748"

# AI Mode Indicator (if you use AI assistants in terminal)
[custom.ai_mode]
disabled = true
detect_files = [".ai", ".cursor", ".claude"]
command = "echo '🧠'"
when = '[ -f .ai ] || [ -f .cursor ] || [ -f .claude ]'
format = "[$output](bold #A78BFA)"
style = "#A78BFA"

# Security Tools Detection (for cybersec work)
[custom.security]
disabled = true
detect_files = ["nuclei", "subfinder", "httpx"]
command = "echo '🛡️'"
when = 'command -v nuclei >/dev/null 2>&1'
format = "[$output](bold #4FD1C5)"

# =====================================================
# 🎯 PERFORMANCE OPTIMIZATIONS
# =====================================================
# Disabled modules for maximum speed:
[aws]
disabled = true

[gcloud]
disabled = true

[terraform]
disabled = true

[vagrant]
disabled = true

[nix_shell]
disabled = true

[conda]
disabled = true

[crystal]
disabled = true

[dart]
disabled = true

[deno]
disabled = true

[dotnet]
disabled = true

[elixir]
disabled = true

[elm]
disabled = true

[erlang]
disabled = true

[haskell]
disabled = true

[helm]
disabled = true

[julia]
disabled = true

[kotlin]
disabled = true

[lua]
disabled = true

[nim]
disabled = true

[ocaml]
disabled = true

[perl]
disabled = true

[php]
disabled = true

[pulumi]
disabled = true

[purescript]
disabled = true

[rlang]
disabled = true

[ruby]
disabled = true

[scala]
disabled = true

[swift]
disabled = true

[zig]
disabled = true

# =====================================================
# 📝 NOTES & USAGE
# =====================================================
#
# To activate this theme:
# 1. cp starship-dreamcoder-verse.toml ~/.config/starship.toml
# 2. source ~/.zshrc
#
# Color Palette (DreamCoder Verse - Soft Productive):
# From kitty/colors-dreamcoder.conf:
# - Cyan Selection: #83d3e3 (username, duration, character)
# - Aqua Green: #7FB3A8 (git added, python, nodejs)
# - Soft Terracotta: #D49A7A (errors, rust)
# - Golden Wheat: #E0C180 (git status, bun)
# - Soft Blue: #7fb3d4 (os, docker)
# - Gentle Lavender: #C5A5D4 (git branch, java, k8s)
# - Cyan Water: #4da6b8 (directory, golang)
#
# Design Philosophy:
# - Single line for maximum screen space
# - Soft colors for reduced eye strain
# - High information density
# - Professional and clean aesthetic
#
# =====================================================
# 🌌 "Code is poetry written in light and logic"
# =====================================================
